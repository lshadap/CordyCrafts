// useClasses placeholder — closes Wave 0 gap for SUPA-03.
// Fuller hook test deferred: @testing-library/react is not installed
// (see 02-RESEARCH.md Environment Availability table).
// Real verification of useClasses behaviour is the manual browser
// check in 02-VALIDATION.md (Classes appear on page after row added).
// Run: npx vitest run useClasses.test

describe('useClasses (placeholder stub)', () => {
  it('todo: real hook test deferred until @testing-library/react is installed', () => {
    // Intentionally trivial — see file header for context.
    expect(true).toBe(true)
  })
})
