---
phase: 01-vite-scaffold
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/primitives.jsx
  - src/hero.jsx
autonomous: true
requirements:
  - SCAF-02

must_haves:
  truths:
    - "src/primitives.jsx exports all 7 Claude Design primitives as named ES module exports"
    - "src/hero.jsx exports Nav, Hero, Categories, CategoryTile as named ES module exports"
    - "Nav accepts cartCount and onOpenCart props — no window.openCart or window.cartCount reads remain"
    - "All asset paths in hero.jsx use absolute /assets/ prefix (leading slash)"
    - "No Object.assign(window, ...) remains in either file"
    - "No component logic, JSX structure, or inline styles are changed"
  artifacts:
    - path: "src/primitives.jsx"
      provides: "Icon, Logo, HeartMark, Button, CircleBadge, Overline, StitchDivider as ES module exports"
      exports: ["Icon", "Logo", "HeartMark", "Button", "CircleBadge", "Overline", "StitchDivider"]
    - path: "src/hero.jsx"
      provides: "Nav, Hero, Categories, CategoryTile as ES module exports"
      exports: ["Nav", "Hero", "Categories", "CategoryTile"]
  key_links:
    - from: "src/hero.jsx"
      to: "src/primitives.jsx"
      via: "import { Icon, Logo, Button, CircleBadge, Overline } from './primitives.jsx'"
      pattern: "from './primitives.jsx'"
    - from: "src/App.jsx (Plan 06)"
      to: "src/hero.jsx Nav"
      via: "cartCount={count()} onOpenCart={() => setCartOpen(true)}"
      pattern: "cartCount.*onOpenCart"
---

<objective>
Migrate primitives.jsx and hero.jsx from CDN/Babel globals to ES modules. These are the two simplest migrations: primitives.jsx needs only 2 changes (add import React, replace Object.assign with export). hero.jsx needs 6 change types: add React + primitive imports, update Nav props signature to replace window globals with props, update asset paths, replace Object.assign with export.

Purpose: primitives.jsx is a dependency of hero.jsx, products.jsx, and sections.jsx — it must migrate first. hero.jsx is not depended on by products.jsx or sections.jsx, so it can run in the same wave.

Output: src/primitives.jsx and src/hero.jsx as ES modules with no window globals.
</objective>

<execution_context>
@/Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.planning/ROADMAP.md
@/Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.planning/phases/01-vite-scaffold/01-CONTEXT.md
@/Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.planning/phases/01-vite-scaffold/01-PATTERNS.md

<interfaces>
<!-- Exact change inventory for primitives.jsx (source: PATTERNS.md) -->

primitives.jsx — exactly 3 changes:
  [1] ADD at line 1:   import React from 'react'
  [2] REMOVE line 124: Object.assign(window, { Icon, Logo, HeartMark, Button, CircleBadge, Overline, StitchDivider });
  [3] ADD (replace):   export { Icon, Logo, HeartMark, Button, CircleBadge, Overline, StitchDivider }
  Everything else (lines 3-122) is UNTOUCHED.

<!-- Exact change inventory for hero.jsx (source: PATTERNS.md) -->

hero.jsx — exactly 6 change types:
  [1] ADD at line 1-2:
      import React from 'react'
      import { Icon, Logo, Button, CircleBadge, Overline } from './primitives.jsx'

  [2] CHANGE line 3 — Nav props signature:
      BEFORE: const Nav = ({ accent }) => (
      AFTER:  const Nav = ({ accent, cartCount = 0, onOpenCart }) => (

  [3] CHANGE line 26 — first window.openCart call:
      BEFORE: onClick={(e) => { if (x.href === '#cart') { e.preventDefault(); window.openCart && window.openCart(); } }}
      AFTER:  onClick={(e) => { if (x.href === '#cart') { e.preventDefault(); onOpenCart && onOpenCart(); } }}

  [4] CHANGE line 33 — second window.openCart call:
      BEFORE: <button onClick={() => window.openCart && window.openCart()} style={{
      AFTER:  <button onClick={() => onOpenCart && onOpenCart()} style={{

  [5] CHANGE line 38 — first window.cartCount:
      BEFORE: {(window.cartCount || 0) > 0 && (
      AFTER:  {(cartCount || 0) > 0 && (

  [6] CHANGE line 45 — second window.cartCount:
      BEFORE: }}>{window.cartCount}</span>
      AFTER:  }}>{cartCount}</span>

  [7] CHANGE line 129 — hero image asset path:
      BEFORE: <img src="assets/hero-fox.svg"
      AFTER:  <img src="/assets/hero-fox.svg"

  [8] CHANGE lines 209-212 — category tile asset paths:
      BEFORE: img="assets/category-amigurumi.svg"
      AFTER:  img="/assets/category-amigurumi.svg"
      (same pattern for category-bags.svg, category-wearables.svg, category-classes.svg)

  [9] REMOVE line 216: Object.assign(window, { Nav, Hero, Categories, CategoryTile });
  [10] ADD (replace):  export { Nav, Hero, Categories, CategoryTile }

  Everything else (all component bodies, JSX structure, inline styles) is UNTOUCHED.

Note: hero.jsx uses React.useState, React.useEffect, React.Fragment — all namespace calls.
Adding `import React from 'react'` is sufficient; no named hook imports needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate src/primitives.jsx — add React import, replace window export</name>
  <read_first>
    - /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/primitives.jsx (the FULL source file — read before making any changes; every line of component code is preserved exactly)
  </read_first>
  <files>src/primitives.jsx</files>
  <action>
Create `src/primitives.jsx` by taking the full content of the root-level `primitives.jsx` and applying exactly 2 changes:

**Change 1: Add import React at line 1**
The file currently starts with a comment (`// Cordy's Crafts — primitives`). Add `import React from 'react'` as the very first line, before the comment.

**Change 2: Replace the last line**
The current last line (line 124) is:
```javascript
Object.assign(window, { Icon, Logo, HeartMark, Button, CircleBadge, Overline, StitchDivider });
```
Replace it with:
```javascript
export { Icon, Logo, HeartMark, Button, CircleBadge, Overline, StitchDivider }
```

**Everything else is copied exactly as-is.** Do not change:
- Any component body (Icon, HeartMark, Logo, Button, CircleBadge, Overline, StitchDivider)
- Any inline style object
- Any SVG path data
- Any CSS variable reference
- Any comment

The file at root (`primitives.jsx`) remains untouched — the migrated version lives at `src/primitives.jsx`. The root file can be removed after all plans complete, but leave it for now to avoid disrupting the CDN prototype (it's harmless).

**Verification before saving:** Count the exports in the new export statement — must be exactly 7:
Icon, Logo, HeartMark, Button, CircleBadge, Overline, StitchDivider
This matches the original Object.assign call exactly.
  </action>
  <verify>
    <automated>cd /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts && head -1 src/primitives.jsx | grep -c "import React from 'react'" && tail -1 src/primitives.jsx | grep -c "export {" && grep -c 'Object.assign' src/primitives.jsx && echo "PRIMITIVES OK"</automated>
  </verify>
  <acceptance_criteria>
    - First line of src/primitives.jsx is exactly: `import React from 'react'`
    - Last line of src/primitives.jsx contains `export {` with all 7 exports: Icon, Logo, HeartMark, Button, CircleBadge, Overline, StitchDivider
    - src/primitives.jsx does NOT contain `Object.assign`
    - src/primitives.jsx does NOT contain `window.`
    - File is 126 lines (original 124 + 1 import line at top, Object.assign replaced not added so line count is 124 + 1 = 125, or 124 + 1 if the comment line is kept before import)
    - `grep -c 'const Icon' src/primitives.jsx` returns 1 (Icon component body untouched)
    - `grep -c 'const Button' src/primitives.jsx` returns 1 (Button component body untouched)
    - CSS variable names like `--cc-font-sans`, `--cc-font-serif`, `--cc-font-script` are present and unchanged
  </acceptance_criteria>
  <done>src/primitives.jsx is a valid ES module exporting all 7 primitives with zero component changes</done>
</task>

<task type="auto">
  <name>Task 2: Migrate src/hero.jsx — imports, Nav props, window globals, asset paths, export</name>
  <read_first>
    - /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/hero.jsx (the FULL source file — read every line before making changes; component bodies are untouched)
    - /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.planning/phases/01-vite-scaffold/01-PATTERNS.md (src/hero.jsx section — exact before/after for each change)
  </read_first>
  <files>src/hero.jsx</files>
  <action>
Create `src/hero.jsx` by applying exactly the 10 changes listed in the interfaces block above. Read the full `hero.jsx` source file first, then make only the listed changes. The file is 216 lines — 200+ lines are untouched.

**Change sequence (apply in order):**

**1. Add import lines at the top (before the existing comment):**
```javascript
import React from 'react'
import { Icon, Logo, Button, CircleBadge, Overline } from './primitives.jsx'
```

**2. Nav props signature (line 3 in original, now lines 5-6 after imports):**
```jsx
// BEFORE:
const Nav = ({ accent }) => (
// AFTER:
const Nav = ({ accent, cartCount = 0, onOpenCart }) => (
```

**3. First window.openCart (link onClick handler, was line 26):**
```jsx
// BEFORE:
onClick={(e) => { if (x.href === '#cart') { e.preventDefault(); window.openCart && window.openCart(); } }}
// AFTER:
onClick={(e) => { if (x.href === '#cart') { e.preventDefault(); onOpenCart && onOpenCart(); } }}
```

**4. Second window.openCart (cart button onClick, was line 33):**
```jsx
// BEFORE:
<button onClick={() => window.openCart && window.openCart()} style={{
// AFTER:
<button onClick={() => onOpenCart && onOpenCart()} style={{
```

**5. First window.cartCount (badge condition, was line 38):**
```jsx
// BEFORE:
{(window.cartCount || 0) > 0 && (
// AFTER:
{(cartCount || 0) > 0 && (
```

**6. Second window.cartCount (badge display, was line 45):**
```jsx
// BEFORE:
}}>{window.cartCount}</span>
// AFTER:
}}>{cartCount}</span>
```

**7. Hero image asset path (was line 129):**
```jsx
// BEFORE:
<img src="assets/hero-fox.svg" alt="Cordy's Crafts featured piece"
// AFTER:
<img src="/assets/hero-fox.svg" alt="Cordy's Crafts featured piece"
```

**8. Category tile asset paths (was lines 209-212, the 4 CategoryTile img= props):**
```jsx
// BEFORE:
img="assets/category-amigurumi.svg"
img="assets/category-bags.svg"
img="assets/category-wearables.svg"
img="assets/category-classes.svg"
// AFTER (add leading slash to each):
img="/assets/category-amigurumi.svg"
img="/assets/category-bags.svg"
img="/assets/category-wearables.svg"
img="/assets/category-classes.svg"
```

**9+10. Replace last line:**
```javascript
// REMOVE:
Object.assign(window, { Nav, Hero, Categories, CategoryTile });
// ADD:
export { Nav, Hero, Categories, CategoryTile }
```

**Verification checklist before saving:**
- `window.openCart` appears 0 times in the new file
- `window.cartCount` appears 0 times in the new file
- `window.` appears 0 times in the new file (no residual window reads)
- `Object.assign` appears 0 times
- `cartCount` appears 2 times (the badge condition + badge display)
- `onOpenCart` appears 2 times (the two click handlers)
- `/assets/` (with leading slash) appears 5 times (hero-fox + 4 categories)
- `assets/` (without leading slash) appears 0 times (all asset paths updated)
  </action>
  <verify>
    <automated>cd /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts && grep -c 'window\.' src/hero.jsx && grep -c 'Object.assign' src/hero.jsx && grep -c 'onOpenCart' src/hero.jsx && grep -c 'cartCount' src/hero.jsx && grep "src=\"assets/" src/hero.jsx | wc -l && echo "HERO OK"</automated>
  </verify>
  <acceptance_criteria>
    - First line: `import React from 'react'`
    - Second line: `import { Icon, Logo, Button, CircleBadge, Overline } from './primitives.jsx'`
    - `grep -c 'window\.' src/hero.jsx` returns 0 (no window globals remain)
    - `grep -c 'Object.assign' src/hero.jsx` returns 0
    - `grep -c 'onOpenCart' src/hero.jsx` returns 2 (two click handlers)
    - `grep -c 'cartCount' src/hero.jsx` returns 3 (prop default + 2 usages) — or 2 if default counts separately
    - `grep 'src="assets/' src/hero.jsx | wc -l` returns 0 (all asset paths have leading slash)
    - `grep 'src="/assets/' src/hero.jsx | wc -l` returns 1 (hero-fox.svg)
    - `grep 'img="/assets/' src/hero.jsx | wc -l` returns 4 (4 category SVGs)
    - Last line: `export { Nav, Hero, Categories, CategoryTile }`
    - Hero component body (JSX structure, all inline styles) is unchanged from the source file
    - `const Nav = ({ accent, cartCount = 0, onOpenCart }) => (` is the exact Nav signature
  </acceptance_criteria>
  <done>src/hero.jsx is a valid ES module; Nav reads cartCount and onOpenCart from props; no window globals remain; all asset paths are absolute</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| onOpenCart prop → CartDrawer state | Callback from Nav triggers cart open in App.jsx |
| /assets/ URL → public/ directory | Asset paths are public, no auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Nav onOpenCart prop | accept | onOpenCart is a callback from App.jsx's useState setter — it can only set cartOpen to true; no user-controlled input is passed through it |
| T-03-02 | Information Disclosure | public/assets/ SVGs | accept | SVG files are decorative product/category images — no PII, no secrets; public accessibility is correct |
| T-03-03 | Spoofing | window.cartCount removed | mitigate | Replacement with cartCount prop from CartContext.Provider ensures the cart badge reflects actual React state, not a window global that any script could manipulate |
</threat_model>

<verification>
After this plan completes:

```bash
# primitives.jsx is a clean ES module
head -1 src/primitives.jsx  # must be: import React from 'react'
tail -1 src/primitives.jsx  # must be: export { Icon, Logo, ... }
grep 'Object.assign' src/primitives.jsx  # must be empty
grep 'window\.' src/primitives.jsx       # must be empty

# hero.jsx has no window globals
grep 'window\.' src/hero.jsx         # must be empty
grep 'Object.assign' src/hero.jsx    # must be empty
grep 'onOpenCart' src/hero.jsx       # must appear 2 times
grep 'cartCount' src/hero.jsx        # must appear (prop + 2 usages)

# Asset paths are absolute
grep "src=\"assets/" src/hero.jsx    # must be empty
grep "img=\"assets/" src/hero.jsx    # must be empty
grep "src=\"/assets/" src/hero.jsx   # must show hero-fox.svg
grep "img=\"/assets/" src/hero.jsx   # must show 4 category SVGs
```
</verification>

<success_criteria>
- src/primitives.jsx: 1 import line added, last line changed to export, zero other changes
- src/hero.jsx: 2 import lines added, Nav signature updated with cartCount + onOpenCart props, 4 window global reads replaced with prop values, 5 asset paths updated to /assets/ prefix, last line changed to export
- Both files: no window. references, no Object.assign
- Neither file has any changes to component logic, JSX structure, or inline style values
</success_criteria>

<output>
After completion, create `.planning/phases/01-vite-scaffold/01-03-SUMMARY.md` following the summary template at the execution_context path above.
</output>
