---
phase: 02-supabase-integration
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/whatsapp.test.js
  - src/__tests__/supabase.test.js
  - src/__tests__/useProducts.test.js
  - src/__tests__/useClasses.test.js
autonomous: true
requirements:
  - WA-01
  - WA-02
  - WA-03
  - SUPA-04
  - SUPA-05

must_haves:
  truths:
    - "src/__tests__/whatsapp.test.js exists and is a failing test stub for buildOrderMessage and buildBookingMessage (will go red until Plan 02 implements src/lib/whatsapp.js)"
    - "src/__tests__/supabase.test.js exists and is a passing pure-function test for the insert-then-gate guard logic (no module imports — guard logic is inlined per the pattern in 02-PATTERNS.md supabase.test.js section)"
    - "src/__tests__/useProducts.test.js and src/__tests__/useClasses.test.js exist as passing placeholder stubs that close the Wave 0 gap referenced by VALIDATION.md (fuller hook tests deferred because @testing-library/react is out of scope per 02-RESEARCH.md Environment Availability)"
    - "npx vitest run prints expected results: whatsapp.test FAILS with import error (file ../lib/whatsapp.js does not exist yet); supabase.test, useProducts.test, useClasses.test all PASS"
  artifacts:
    - path: "src/__tests__/whatsapp.test.js"
      provides: "WA-01, WA-02, WA-03 behavioral contract — defines what buildOrderMessage and buildBookingMessage must produce"
      contains: "buildOrderMessage"
    - path: "src/__tests__/supabase.test.js"
      provides: "SUPA-04, SUPA-05 insert-then-gate guard contract — defines that error → ok:false, no error → ok:true"
      contains: "insert-then-gate"
    - path: "src/__tests__/useProducts.test.js"
      provides: "SUPA-02 placeholder stub — closes Wave 0 gap; fuller hook tests deferred (no @testing-library/react)"
      contains: "useProducts"
    - path: "src/__tests__/useClasses.test.js"
      provides: "SUPA-03 placeholder stub — closes Wave 0 gap; fuller hook tests deferred (no @testing-library/react)"
      contains: "useClasses"
  key_links:
    - from: "src/__tests__/whatsapp.test.js"
      to: "src/lib/whatsapp.js"
      via: "import { buildOrderMessage, buildBookingMessage } from '../lib/whatsapp.js'"
      pattern: "from '../lib/whatsapp"
    - from: "src/__tests__/supabase.test.js"
      to: "(self-contained guard logic — no production import)"
      via: "inlined async submitOrder/submitBooking functions accepting a mock supabase"
      pattern: "submitOrder.*supabase"
---

<objective>
Create two failing-stub test files in src/__tests__/ that lock the behavioral contract for the WhatsApp message builders (WA-01, WA-02, WA-03) and the insert-then-gate guard (SUPA-04, SUPA-05). These tests are the canonical source of truth for what Plan 02 (whatsapp.js) and Plans 03 must produce.

Purpose: Wave 0 establishes the validation harness. Per 02-VALIDATION.md, every Phase 2 task must verify against an automated test that already exists. Without these stubs, downstream tasks have no <automated> command to run.

Output: Two test files that vitest discovers and runs. The whatsapp.test.js will fail until Plan 02 ships `src/lib/whatsapp.js` (RED→GREEN handoff). The supabase.test.js will pass immediately because its guard logic is inlined in the test file (testing the pattern, not the wiring).
</objective>

<execution_context>
@/Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/CLAUDE.md
@/Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.planning/phases/02-supabase-integration/02-CONTEXT.md
@/Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.planning/phases/02-supabase-integration/02-PATTERNS.md
@/Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.planning/phases/02-supabase-integration/02-VALIDATION.md

<interfaces>
<!-- Vitest config and existing test file conventions -->

vitest.config.js sets `include: ['src/__tests__/**/*.test.{js,jsx}']` and `globals: true`.
Tests MUST live in `src/__tests__/` (NOT `src/lib/__tests__/`) to be discovered.

Existing test pattern from src/__tests__/CartContext.test.jsx:
```js
import { cartReducer } from '../CartContext.jsx'

describe('cartReducer — add', () => {
  it('adds a new item with qty=1 when sku not present', () => {
    const result = cartReducer([], { type: 'ADD', item: item1 })
    expect(result).toHaveLength(1)
  })
})
```

Locked WhatsApp message format (from 02-CONTEXT.md):
- Order: "New Order\nName: ...\nPhone: ...\nAddress: ...\n\nItems:\n- {name} x{qty} ₹{lineTotal}\n\nTotal: ₹{grand}"
- Booking: "New Booking\nClass: ...\nName: ...\nPhone: ...\nSeats: ...\nMode: ...\n[Notes: ...]"  (omit Notes line when empty)
- wa.me URL format: `https://wa.me/{VITE_WHATSAPP_NUMBER}?text={encodeURIComponent(msg)}`

Locked guard contract (from 02-CONTEXT.md):
- Failed INSERT → no wa.me redirect, inline red error "Something went wrong — please try again."
- Successful INSERT → open wa.me, then clear cart / set done stage
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/__tests__/whatsapp.test.js — failing stub for WA-01, WA-02, WA-03</name>
  <read_first>
    - /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/src/__tests__/CartContext.test.jsx (existing test pattern — describe/it/expect with vitest globals, import from relative path)
    - /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.planning/phases/02-supabase-integration/02-PATTERNS.md (whatsapp.test.js section lines 370-432 — exact test cases to encode)
    - /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.planning/phases/02-supabase-integration/02-CONTEXT.md (Area 4 — locked WhatsApp message format)
  </read_first>
  <files>src/__tests__/whatsapp.test.js</files>
  <action>
Create `src/__tests__/whatsapp.test.js` with the EXACT content below. Tests import `buildOrderMessage`, `buildBookingMessage`, and `buildWaUrl` from `../lib/whatsapp.js` — that module does NOT exist yet. The import error is intentional: it will fail vitest until Plan 02 creates the module. This is the RED step.

Do NOT create `src/lib/whatsapp.js` in this plan. Plan 02 owns that file.

Exact file content:

```js
// WhatsApp message builder tests — covers WA-01, WA-02, WA-03
// RED until Plan 02 ships src/lib/whatsapp.js
// Run: npx vitest run whatsapp.test

import { buildOrderMessage, buildBookingMessage, buildWaUrl } from '../lib/whatsapp.js'

const orderForm = { name: 'Priya', phone: '9876543210', addr: '12 MG Road, Bengaluru' }
const orderItems = [
  { sku: 'p01', name: 'Stamped Birthday Card', qty: 2, price: 180 },
  { sku: 'k01', name: 'Rose Garden Soy Candle', qty: 1, price: 650 },
]
const orderTotal = 1010

describe('buildOrderMessage — WA-01', () => {
  it('starts with "New Order" header', () => {
    expect(buildOrderMessage(orderForm, orderItems, orderTotal).startsWith('New Order')).toBe(true)
  })
  it('contains customer name line', () => {
    expect(buildOrderMessage(orderForm, orderItems, orderTotal)).toContain('Name: Priya')
  })
  it('contains phone line', () => {
    expect(buildOrderMessage(orderForm, orderItems, orderTotal)).toContain('Phone: 9876543210')
  })
  it('contains address line when address present', () => {
    expect(buildOrderMessage(orderForm, orderItems, orderTotal)).toContain('Address: 12 MG Road, Bengaluru')
  })
  it('renders address fallback when address empty', () => {
    const formNoAddr = { ...orderForm, addr: '' }
    expect(buildOrderMessage(formNoAddr, orderItems, orderTotal)).toContain('Address: Not provided')
  })
  it('contains item lines with qty and line total in rupees', () => {
    const msg = buildOrderMessage(orderForm, orderItems, orderTotal)
    expect(msg).toContain('- Stamped Birthday Card x2 ₹360')
    expect(msg).toContain('- Rose Garden Soy Candle x1 ₹650')
  })
  it('contains grand total line in rupees', () => {
    expect(buildOrderMessage(orderForm, orderItems, orderTotal)).toContain('Total: ₹1010')
  })
})

const bookingKlass = { id: 'uuid-abc-123', name: 'Card Making with Stamps' }
const bookingFormBase = { name: 'Priya', phone: '9876543210', seats: 2, pref: 'Online', note: '' }

describe('buildBookingMessage — WA-02', () => {
  it('starts with "New Booking" header', () => {
    expect(buildBookingMessage(bookingFormBase, bookingKlass).startsWith('New Booking')).toBe(true)
  })
  it('contains class name line', () => {
    expect(buildBookingMessage(bookingFormBase, bookingKlass)).toContain('Class: Card Making with Stamps')
  })
  it('contains customer name line', () => {
    expect(buildBookingMessage(bookingFormBase, bookingKlass)).toContain('Name: Priya')
  })
  it('contains seats line', () => {
    expect(buildBookingMessage(bookingFormBase, bookingKlass)).toContain('Seats: 2')
  })
  it('contains mode line', () => {
    expect(buildBookingMessage(bookingFormBase, bookingKlass)).toContain('Mode: Online')
  })
  it('omits Notes line when note is empty string', () => {
    expect(buildBookingMessage(bookingFormBase, bookingKlass)).not.toContain('Notes:')
  })
  it('includes Notes line when note has content', () => {
    const formWithNote = { ...bookingFormBase, note: 'Veg food only' }
    expect(buildBookingMessage(formWithNote, bookingKlass)).toContain('Notes: Veg food only')
  })
})

describe('buildWaUrl — WA-03', () => {
  it('uses VITE_WHATSAPP_NUMBER from import.meta.env (not hardcoded)', () => {
    // import.meta.env.VITE_WHATSAPP_NUMBER is injected by Vite at test time from .env / vitest config
    // The function must read it dynamically, not embed a literal number
    const url = buildWaUrl('hello world')
    expect(url.startsWith('https://wa.me/')).toBe(true)
    // Number must come from env — assert the URL contains whatever the env provides
    expect(url).toContain(import.meta.env.VITE_WHATSAPP_NUMBER || '')
  })
  it('encodes the message via encodeURIComponent (spaces become %20 or +)', () => {
    const url = buildWaUrl('hello world ₹100')
    // encodeURIComponent encodes space as %20 and ₹ as %E2%82%B9
    expect(url).toContain('text=hello%20world')
    expect(url).toContain('%E2%82%B9100')
  })
  it('preserves newlines in encoded form (%0A)', () => {
    const url = buildWaUrl('line1\nline2')
    expect(url).toContain('line1%0Aline2')
  })
})
```

Key notes for the executor:
1. The file uses vitest globals (describe, it, expect) — vitest.config.js has `globals: true`, so no `import { describe } from 'vitest'` needed. Match the existing CartContext.test.jsx pattern exactly.
2. The import path is `'../lib/whatsapp.js'` — that resolves to `src/lib/whatsapp.js` from `src/__tests__/whatsapp.test.js`. The file does not exist yet — that is the point.
3. Do NOT add a `vitest.setup.js` env injection. import.meta.env.VITE_WHATSAPP_NUMBER will be undefined in tests unless a `.env.test` is provided; the test uses `|| ''` to defensively handle that case while still verifying the URL prefix is correct. The behavioral assertion is "buildWaUrl uses whatever import.meta.env.VITE_WHATSAPP_NUMBER returns".
4. After writing the file, run `npx vitest run whatsapp.test` and confirm the FAILURE is `Cannot find module '../lib/whatsapp.js'` or equivalent module-resolution error. That confirms the test file is structurally correct and the only blocker is the missing implementation. Do NOT mark this task complete if the failure is a syntax error or different cause.
  </action>
  <verify>
    <automated>cd /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts && test -f src/__tests__/whatsapp.test.js && grep -q "buildOrderMessage" src/__tests__/whatsapp.test.js && grep -q "buildBookingMessage" src/__tests__/whatsapp.test.js && grep -q "buildWaUrl" src/__tests__/whatsapp.test.js && grep -q "from '../lib/whatsapp.js'" src/__tests__/whatsapp.test.js && (npx vitest run whatsapp.test 2>&1 | grep -E "Cannot find|Failed to resolve|whatsapp.js" >/dev/null) && echo "RED OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/__tests__/whatsapp.test.js` exists
    - File contains the literal string `from '../lib/whatsapp.js'`
    - File contains 3 describe blocks: `buildOrderMessage — WA-01`, `buildBookingMessage — WA-02`, `buildWaUrl — WA-03`
    - File contains the literal substring `'- Stamped Birthday Card x2 ₹360'` (locks the rendering format including the dash, x notation, ₹ symbol, and qty*price multiplication)
    - File contains the literal substring `'Total: ₹1010'` (locks grand total format)
    - File contains the literal substring `'Notes: Veg food only'` (locks conditional Notes inclusion)
    - File does NOT contain `import { describe }` or `import { it }` (uses vitest globals, matching CartContext.test.jsx)
    - `npx vitest run whatsapp.test` fails with a module-resolution error mentioning `whatsapp.js` (the only legitimate failure mode for Wave 0; Plan 02 turns this green)
  </acceptance_criteria>
  <done>whatsapp.test.js committed; vitest run produces a module-not-found error for ../lib/whatsapp.js — RED state confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/__tests__/supabase.test.js — passing stub for the insert-then-gate guard (SUPA-04, SUPA-05)</name>
  <read_first>
    - /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/src/__tests__/main.test.jsx (existing pattern of inline-logic-extraction tests with no production imports)
    - /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.planning/phases/02-supabase-integration/02-PATTERNS.md (supabase.test.js section lines 436-502 — exact test structure)
    - /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.planning/phases/02-supabase-integration/02-RESEARCH.md (Pitfall 1 — INSERT returns {data: null, error: null} on success; Pattern 2 — async submit handler)
  </read_first>
  <files>src/__tests__/supabase.test.js</files>
  <action>
Create `src/__tests__/supabase.test.js` with the EXACT content below. Unlike whatsapp.test.js, this file has NO production imports — it inlines the guard logic as local async functions and tests them with mock supabase clients. This pattern (from 02-RESEARCH.md and 02-PATTERNS.md) is mandated because installing @testing-library/react is out of scope and the React component context cannot be tested without it.

The file passes immediately. The behavioral contract it locks is consumed by Plan 03 — the executor of Plan 03 is responsible for ensuring CartDrawer/BookingModal submit handlers follow exactly the same `if (error) return; window.open(...)` pattern.

Exact file content:

```js
// Insert-then-gate guard tests — covers SUPA-04, SUPA-05
// Pattern source: 02-PATTERNS.md "supabase.test.js" section
// Plan 03 must wire CartDrawer/BookingModal submit handlers using the SAME guard pattern verified here.
// Run: npx vitest run supabase.test

// Local guard functions — mirror exactly what CartDrawer/BookingModal will do.
// These are NOT exported. Phase 2 deliberately tests the PATTERN, not the wiring,
// because @testing-library/react is not installed (out-of-scope per RESEARCH.md
// Environment Availability table).
async function submitOrder(supabase, payload) {
  const { error } = await supabase.from('orders').insert(payload)
  if (error) return { ok: false, error: error.message }
  return { ok: true, error: null }
}

async function submitBooking(supabase, payload) {
  const { error } = await supabase.from('bookings').insert(payload)
  if (error) return { ok: false, error: error.message }
  return { ok: true, error: null }
}

const fakeOrderPayload = {
  customer_name: 'Priya',
  customer_whatsapp: '9876543210',
  address: '12 MG Road',
  items: [{ sku: 'p01', name: 'Card', qty: 2, price: 180 }],
  total_amount: 360,
  payment_status: 'pending',
}

const fakeBookingPayload = {
  class_id: 'uuid-abc-123',
  customer_name: 'Priya',
  customer_whatsapp: '9876543210',
  seats: 2,
  mode_preference: 'Online',
  message: null,
  payment_status: 'pending',
}

describe('insert-then-gate guard — submitOrder (SUPA-04)', () => {
  it('returns ok:true when supabase insert returns no error (success path)', async () => {
    const mockSupabase = { from: () => ({ insert: async () => ({ data: null, error: null }) }) }
    const result = await submitOrder(mockSupabase, fakeOrderPayload)
    expect(result.ok).toBe(true)
    expect(result.error).toBe(null)
  })

  it('returns ok:false when supabase insert returns an error (failure path — no wa.me)', async () => {
    const mockSupabase = { from: () => ({ insert: async () => ({ data: null, error: { message: 'permission denied' } }) }) }
    const result = await submitOrder(mockSupabase, fakeOrderPayload)
    expect(result.ok).toBe(false)
    expect(result.error).toBe('permission denied')
  })

  it('checks error not data — Supabase v2 insert without .select() returns data:null even on success', async () => {
    // Pitfall 1 from RESEARCH.md: developers may incorrectly check `if (!data)`.
    // The guard must check `error` only.
    const mockSupabase = { from: () => ({ insert: async () => ({ data: null, error: null }) }) }
    const result = await submitOrder(mockSupabase, fakeOrderPayload)
    // data is null even though insert succeeded — ok must still be true
    expect(result.ok).toBe(true)
  })

  it('passes the payload through to supabase.from().insert() unchanged', async () => {
    let captured = null
    const mockSupabase = {
      from: (table) => ({
        insert: async (payload) => {
          captured = { table, payload }
          return { data: null, error: null }
        },
      }),
    }
    await submitOrder(mockSupabase, fakeOrderPayload)
    expect(captured.table).toBe('orders')
    expect(captured.payload).toEqual(fakeOrderPayload)
    expect(captured.payload.payment_status).toBe('pending')
  })
})

describe('insert-then-gate guard — submitBooking (SUPA-05)', () => {
  it('returns ok:true on successful booking insert', async () => {
    const mockSupabase = { from: () => ({ insert: async () => ({ data: null, error: null }) }) }
    const result = await submitBooking(mockSupabase, fakeBookingPayload)
    expect(result.ok).toBe(true)
  })

  it('returns ok:false on FK violation (e.g., class_id not in classes table)', async () => {
    const mockSupabase = { from: () => ({ insert: async () => ({ data: null, error: { message: 'foreign key violation' } }) }) }
    const result = await submitBooking(mockSupabase, fakeBookingPayload)
    expect(result.ok).toBe(false)
    expect(result.error).toBe('foreign key violation')
  })

  it('inserts into the bookings table (not orders)', async () => {
    let capturedTable = null
    const mockSupabase = {
      from: (table) => ({
        insert: async () => {
          capturedTable = table
          return { data: null, error: null }
        },
      }),
    }
    await submitBooking(mockSupabase, fakeBookingPayload)
    expect(capturedTable).toBe('bookings')
  })

  it('payload includes class_id (FK to classes.id), not class_sku', async () => {
    // Pitfall 4 from RESEARCH.md — must use klass.id not klass.sku
    expect(fakeBookingPayload.class_id).toBeDefined()
    expect(fakeBookingPayload.class_sku).toBeUndefined()
  })
})
```

Key notes for the executor:
1. This file has NO `import` statements. It uses vitest globals only.
2. The functions `submitOrder` and `submitBooking` are LOCAL to this test file — they are not exported and not imported from production code. This is by design (see RESEARCH.md Environment Availability).
3. After writing the file, run `npx vitest run supabase.test` and confirm all describe blocks pass GREEN. Unlike whatsapp.test.js, this file has no missing dependency.
  </action>
  <verify>
    <automated>cd /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts && test -f src/__tests__/supabase.test.js && grep -q "submitOrder" src/__tests__/supabase.test.js && grep -q "submitBooking" src/__tests__/supabase.test.js && grep -q "permission denied" src/__tests__/supabase.test.js && grep -q "foreign key violation" src/__tests__/supabase.test.js && ! grep -q "^import" src/__tests__/supabase.test.js && npx vitest run supabase.test 2>&1 | grep -E "passed|✓" >/dev/null && echo "GREEN OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/__tests__/supabase.test.js` exists
    - File contains 2 describe blocks: `insert-then-gate guard — submitOrder (SUPA-04)` and `insert-then-gate guard — submitBooking (SUPA-05)`
    - File contains the local async function definition `async function submitOrder(supabase, payload)` (inlined guard, not imported)
    - File contains the local async function definition `async function submitBooking(supabase, payload)`
    - File has zero `import` statements (uses vitest globals; no production module imports)
    - File contains `fakeBookingPayload.class_id` reference (locks Pitfall 4 — FK uses class_id not class_sku)
    - File contains an assertion that `data: null, error: null` on success path returns `ok: true` (locks Pitfall 1 — check error not data)
    - `npx vitest run supabase.test` exits 0 with all tests passing — supabase.test.js is GREEN immediately on creation
  </acceptance_criteria>
  <done>supabase.test.js committed; npx vitest run supabase.test passes all assertions on first run.</done>
</task>

<task type="auto">
  <name>Task 3: Create src/__tests__/useProducts.test.js — placeholder stub for SUPA-02</name>
  <read_first>
    - /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.planning/phases/02-supabase-integration/02-VALIDATION.md (Wave 0 Requirements — confirms this stub closes the SUPA-02 gap)
    - /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.planning/phases/02-supabase-integration/02-RESEARCH.md (Environment Availability — explains why fuller hook tests are out of scope)
  </read_first>
  <files>src/__tests__/useProducts.test.js</files>
  <action>
Create `src/__tests__/useProducts.test.js` as a passing placeholder stub. The file's only job is to close the Wave 0 gap referenced in VALIDATION.md (`npx vitest run useProducts.test`). Fuller hook tests (mounting + asserting return values) require `@testing-library/react`, which is NOT installed (per 02-RESEARCH.md Environment Availability) and is out of scope for Phase 2.

Do NOT actually mount the hook here. The placeholder uses a single `it('todo', () => {})` that passes trivially. Plan 02 ships the real `useProducts` implementation with manual browser verification per VALIDATION.md Manual-Only Verifications row "Products appear on page after row added".

Exact file content:

```js
// useProducts placeholder — closes Wave 0 gap for SUPA-02.
// Fuller hook test deferred: @testing-library/react is not installed
// (see 02-RESEARCH.md Environment Availability table).
// Real verification of useProducts behaviour is the manual browser
// check in 02-VALIDATION.md (Products appear on page after row added).
// Run: npx vitest run useProducts.test

describe('useProducts (placeholder stub)', () => {
  it('todo: real hook test deferred until @testing-library/react is installed', () => {
    // Intentionally trivial — see file header for context.
    expect(true).toBe(true)
  })
})
```

Notes:
1. No `import` statements. Vitest globals only (matches CartContext.test.jsx, main.test.jsx, supabase.test.js style).
2. Do NOT import `useProducts` from `../hooks/useProducts.js` here — that file does not exist until Plan 02. Importing it would re-introduce the same RED-on-Plan-00 problem that whatsapp.test.js intentionally has, and we already have one RED test serving that role. This stub stays GREEN.
3. After writing, run `npx vitest run useProducts.test` — must pass.
  </action>
  <verify>
    <automated>cd /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts && test -f src/__tests__/useProducts.test.js && grep -q "useProducts (placeholder stub)" src/__tests__/useProducts.test.js && ! grep -q "^import" src/__tests__/useProducts.test.js && npx vitest run useProducts.test 2>&1 | grep -qE "passed|✓" && echo "USEPRODUCTS STUB OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/__tests__/useProducts.test.js` exists
    - File contains the literal string `useProducts (placeholder stub)` (so VALIDATION.md `grep` confirms file scope)
    - File has zero `import` statements (uses vitest globals; no production dependency)
    - `npx vitest run useProducts.test` exits 0 with the placeholder passing
  </acceptance_criteria>
  <done>useProducts.test.js committed as a passing placeholder; Wave 0 SUPA-02 gap closed.</done>
</task>

<task type="auto">
  <name>Task 4: Create src/__tests__/useClasses.test.js — placeholder stub for SUPA-03</name>
  <read_first>
    - /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.planning/phases/02-supabase-integration/02-VALIDATION.md (Wave 0 Requirements — confirms this stub closes the SUPA-03 gap)
    - /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/src/__tests__/useProducts.test.js (just-created — same pattern; copy structure)
  </read_first>
  <files>src/__tests__/useClasses.test.js</files>
  <action>
Create `src/__tests__/useClasses.test.js` with the same placeholder pattern as Task 3. Closes the Wave 0 gap for SUPA-03 referenced in VALIDATION.md (`npx vitest run useClasses.test`).

Exact file content:

```js
// useClasses placeholder — closes Wave 0 gap for SUPA-03.
// Fuller hook test deferred: @testing-library/react is not installed
// (see 02-RESEARCH.md Environment Availability table).
// Real verification of useClasses behaviour is the manual browser
// check in 02-VALIDATION.md (Classes appear on page after row added).
// Run: npx vitest run useClasses.test

describe('useClasses (placeholder stub)', () => {
  it('todo: real hook test deferred until @testing-library/react is installed', () => {
    // Intentionally trivial — see file header for context.
    expect(true).toBe(true)
  })
})
```

Same notes as Task 3: no imports, vitest globals only, no reference to `../hooks/useClasses.js` (that file does not exist until Plan 02).
  </action>
  <verify>
    <automated>cd /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts && test -f src/__tests__/useClasses.test.js && grep -q "useClasses (placeholder stub)" src/__tests__/useClasses.test.js && ! grep -q "^import" src/__tests__/useClasses.test.js && npx vitest run useClasses.test 2>&1 | grep -qE "passed|✓" && echo "USECLASSES STUB OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/__tests__/useClasses.test.js` exists
    - File contains the literal string `useClasses (placeholder stub)`
    - File has zero `import` statements
    - `npx vitest run useClasses.test` exits 0 with the placeholder passing
  </acceptance_criteria>
  <done>useClasses.test.js committed as a passing placeholder; Wave 0 SUPA-03 gap closed.</done>
</task>

<task type="auto">
  <name>Task 5: Sanity-check the test directory and overall vitest run</name>
  <read_first>
    - /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/vitest.config.js (confirm `include` glob covers src/__tests__/)
    - /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/src/__tests__/whatsapp.test.js (just-created file)
    - /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/src/__tests__/supabase.test.js (just-created file)
  </read_first>
  <files>(no file modifications — verification-only task)</files>
  <action>
Run the full vitest suite and confirm the expected mixed state:

```bash
npx vitest run
```

Expected outcome:
- CartContext.test.jsx: PASS (existing from Phase 1)
- main.test.jsx: PASS (existing from Phase 1)
- supabase.test.js: PASS (created in Task 2)
- useProducts.test.js: PASS (created in Task 3 — placeholder)
- useClasses.test.js: PASS (created in Task 4 — placeholder)
- whatsapp.test.js: FAIL with module-resolution error pointing at `../lib/whatsapp.js` (created in Task 1, intentionally RED)

If any test other than whatsapp.test.js fails, STOP and investigate before completing the task. Do NOT proceed to writing the SUMMARY if Phase 1 tests have regressed.

If whatsapp.test.js fails for any reason OTHER than module resolution (e.g., parsing error, assertion error), STOP and fix the test file in Task 1 before completing this plan.

Document the per-file pass/fail outcome in the plan summary at `.planning/phases/02-supabase-integration/02-00-SUMMARY.md` (created by the execute-plan workflow). Specifically record: which tests are RED, which are GREEN, and confirm that the only RED test is whatsapp.test.js.
  </action>
  <verify>
    <automated>cd /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts && OUT=$(npx vitest run 2>&1); echo "$OUT" | grep -E "CartContext.test.jsx|main.test.jsx|supabase.test.js|whatsapp.test.js" && echo "$OUT" | grep -q "FAIL.*whatsapp.test" && (echo "$OUT" | grep -q "FAIL.*CartContext\|FAIL.*main\|FAIL.*supabase" && echo "UNEXPECTED REGRESSION" && exit 1 || echo "RED-GREEN HANDOFF OK")</automated>
  </verify>
  <acceptance_criteria>
    - vitest discovers all 6 test files (CartContext, main, supabase, whatsapp, useProducts, useClasses)
    - whatsapp.test.js is the ONLY failing test file
    - whatsapp.test.js failure is a module-resolution error referencing `whatsapp.js` (not a syntax/parse error)
    - CartContext.test.jsx, main.test.jsx, supabase.test.js, useProducts.test.js, and useClasses.test.js all pass — Phase 1 tests have not regressed and Wave 0 stubs are green
  </acceptance_criteria>
  <done>Full vitest run shows exactly one FAIL (whatsapp.test.js — module not found); all other tests GREEN. Wave 0 RED→GREEN handoff to Plan 02 confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → filesystem | vitest reads .js files from src/__tests__/; no untrusted input crosses this boundary in test files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-00-01 | Information Disclosure | Test fixtures (fake phone, fake name) | accept | Test fixtures use clearly-fake data ('Priya', '9876543210', 'Test ...') — no real PII; documented convention |
| T-02-00-02 | Tampering | Mocked supabase object exposes fake error messages | accept | Error messages in mocks ('permission denied', 'foreign key violation') are fixture strings, not propagated to production code paths |
</threat_model>

<verification>
After this plan completes, verify Wave 0 state:

```bash
# All four test files exist
test -f src/__tests__/whatsapp.test.js
test -f src/__tests__/supabase.test.js
test -f src/__tests__/useProducts.test.js
test -f src/__tests__/useClasses.test.js

# whatsapp.test.js is RED with the expected reason
npx vitest run whatsapp.test 2>&1 | grep -E "Cannot find|Failed to resolve|whatsapp.js"

# supabase.test.js, useProducts.test.js, useClasses.test.js are GREEN
npx vitest run supabase.test 2>&1 | grep -q "passed"
npx vitest run useProducts.test 2>&1 | grep -q "passed"
npx vitest run useClasses.test 2>&1 | grep -q "passed"

# Phase 1 tests have not regressed
npx vitest run CartContext.test 2>&1 | grep -q "passed"
npx vitest run main.test 2>&1 | grep -q "passed"
```
</verification>

<success_criteria>
- src/__tests__/whatsapp.test.js exists, encodes WA-01/WA-02/WA-03 contracts, fails with module-not-found
- src/__tests__/supabase.test.js exists, encodes SUPA-04/SUPA-05 guard pattern, passes immediately
- src/__tests__/useProducts.test.js and src/__tests__/useClasses.test.js exist as passing placeholder stubs (close VALIDATION.md Wave 0 SUPA-02/SUPA-03 gaps)
- All Phase 1 tests still pass — no regression
- Plan 02 has a clear "make whatsapp.test.js green" target via the existing assertions
- Plan 03 has a clear "implement the guard pattern verified in supabase.test.js" target
</success_criteria>

<output>
After completion, create `.planning/phases/02-supabase-integration/02-00-SUMMARY.md` following the summary template at the execution_context path above.
</output>
