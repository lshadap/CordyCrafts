---
phase: 03-mobile-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - PLSH-05
  - PLSH-06

must_haves:
  truths:
    - "The page has a <title> and <meta name='description'> set for search engines"
    - "The page has og:title, og:description, og:image, and og:url Open Graph tags for social sharing"
    - "The page has Twitter Card tags so link previews render correctly on Twitter/X"
    - "A @keyframes shimmer CSS animation is defined in the <head> so skeleton cards can animate"
  artifacts:
    - path: "index.html"
      provides: "SEO meta tags, OG tags, Twitter Card tags, @keyframes shimmer"
      contains: "og:image"
  key_links:
    - from: "index.html <style> @keyframes shimmer"
      to: "src/products.jsx skeleton cards"
      via: "animation: 'shimmer 1.4s infinite linear' inline style"
      pattern: "keyframes shimmer"
---

<objective>
Extend `index.html` `<head>` with static SEO meta tags, Open Graph tags, Twitter Card tags, and a `<style>` block containing `@keyframes shimmer`.

Purpose: Satisfies PLSH-05 (SEO title + description) and PLSH-06 (og:title, og:description, og:image). The `@keyframes shimmer` block is the prerequisite for the skeleton card animation added in Plan 02 — CSS keyframes cannot be expressed as inline styles, so the rule must live in a `<style>` tag in `<head>`.
Output: Updated `index.html` with all meta and style additions in `<head>`. No other part of the file changes.
</objective>

<execution_context>
@/Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.planning/PROJECT.md
@/Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.planning/ROADMAP.md
@/Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.planning/STATE.md

<interfaces>
<!-- Current index.html — full file (12 lines) -->

```html
<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<title>Cordy's Crafts · Hand-made with love</title>
<link rel="stylesheet" href="/colors_and_type.css">
</head>
<body>
<div id="root"></div>
<script type="module" src="/src/main.jsx"></script>
</body>
</html>
```

Key facts:
- There is NO existing <style> block — the @keyframes shimmer creates the first one
- The existing <title> is kept exactly as-is ("Cordy's Crafts · Hand-made with love")
- The <body> and <script> blocks are untouched
- og:image placeholder URL per D-07: https://cordyscrafts.vercel.app/assets/cordys-logo.png
  (domain confirmed + updated in Phase 4)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SEO/OG/Twitter meta tags and @keyframes shimmer to index.html</name>
  <files>index.html</files>

  <read_first>
    - index.html — read the full file before editing (it is 12 lines; read all of it)
    - .planning/phases/03-mobile-polish/03-CONTEXT.md — D-03 (shimmer keyframe), D-06 (static tags), D-07 (og:image URL)
    - .planning/phases/03-mobile-polish/03-PATTERNS.md — index.html section with exact HTML block to insert
  </read_first>

  <action>
Rewrite `index.html` to the following exact content. The existing `<title>` and `<link rel="stylesheet">` are preserved unchanged. All new content is inserted between the existing `<link>` line and the closing `</head>` tag:

```html
<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Cordy's Crafts · Hand-made with love</title>
<meta name="description" content="Cordy's Crafts — handmade paper crafts, clay, and candles from Cordeelia's studio in India. Shop online or book a small-group workshop.">
<link rel="stylesheet" href="/colors_and_type.css">

<!-- Open Graph -->
<meta property="og:type" content="website">
<meta property="og:title" content="Cordy's Crafts · Hand-made with love">
<meta property="og:description" content="Handmade paper crafts, clay, and candles. Shop or book a workshop.">
<meta property="og:image" content="https://cordyscrafts.vercel.app/assets/cordys-logo.png">
<meta property="og:url" content="https://cordyscrafts.vercel.app/">

<!-- Twitter Card -->
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="Cordy's Crafts · Hand-made with love">
<meta name="twitter:description" content="Handmade paper crafts, clay, and candles. Shop or book a workshop.">
<meta name="twitter:image" content="https://cordyscrafts.vercel.app/assets/cordys-logo.png">

<!-- Skeleton shimmer animation (used by ProductGrid and ClassesGrid loading states) -->
<style>
@keyframes shimmer {
  from { background-position: -800px 0; }
  to   { background-position:  800px 0; }
}
</style>
</head>
<body>
<div id="root"></div>
<script type="module" src="/src/main.jsx"></script>
</body>
</html>
```

Do not change anything in `<body>`. The `<title>` value is preserved exactly. The `og:image` URL uses the placeholder domain `https://cordyscrafts.vercel.app` per D-07 — Phase 4 will confirm the real domain.
  </action>

  <verify>
    <automated>grep -c "og:image" /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/index.html</automated>
  </verify>

  <acceptance_criteria>
    - `index.html` contains `<meta name="viewport" content="width=device-width, initial-scale=1">`
    - `index.html` contains `<meta name="description" content="Cordy's Crafts`
    - `index.html` contains `<meta property="og:title" content="Cordy's Crafts`
    - `index.html` contains `<meta property="og:description"`
    - `index.html` contains `<meta property="og:image" content="https://cordyscrafts.vercel.app/assets/cordys-logo.png">`
    - `index.html` contains `<meta property="og:url"`
    - `index.html` contains `<meta name="twitter:card" content="summary_large_image">`
    - `index.html` contains `<meta name="twitter:title"`
    - `index.html` contains `<meta name="twitter:image"`
    - `index.html` contains `@keyframes shimmer`
    - `index.html` contains `background-position: -800px 0`
    - `index.html` contains `background-position:  800px 0`
    - `index.html` still contains `<title>Cordy's Crafts · Hand-made with love</title>` (unchanged)
    - `index.html` still contains `<link rel="stylesheet" href="/colors_and_type.css">` (unchanged)
    - `index.html` still contains `<div id="root"></div>` (unchanged)
    - `index.html` still contains `<script type="module" src="/src/main.jsx"></script>` (unchanged)
  </acceptance_criteria>

  <done>index.html head contains viewport meta, description meta, full OG block (og:type, og:title, og:description, og:image, og:url), full Twitter Card block, and @keyframes shimmer in a style tag. Body is untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    SEO and OG meta tags added to index.html. @keyframes shimmer defined. Run `npm run dev` and verify the tags appear in the browser's page source, and confirm the shimmer animation plays on the loading skeleton (if you can temporarily force loading=true by throttling Supabase in Network DevTools).
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` in the project root
    2. Open http://localhost:5173 in a browser
    3. Open DevTools → Elements → `<head>` — confirm `<meta property="og:title">` and `<meta name="description">` are present
    4. Open DevTools → Network → throttle to "Slow 3G" or "Offline", then refresh
    5. Confirm shimmer skeleton cards appear in the product and classes sections (animated gradient sweep)
    6. Un-throttle — confirm real cards load after data returns
    7. Confirm the page title in the browser tab reads "Cordy's Crafts · Hand-made with love"
  </how-to-verify>
  <resume-signal>Type "approved" if meta tags are present and skeleton animation plays, or describe any issue</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| index.html → browser | Static HTML served to all visitors; no user input, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | og:image URL in index.html | accept | URL points to a public logo asset. No secrets or PII in meta tags. The placeholder domain `cordyscrafts.vercel.app` is the intended production URL. |
| T-03-03-02 | Tampering | @keyframes shimmer CSS | accept | Client-side CSS only; no user input processed. A malicious actor can modify their local DOM but this does not affect other users. |
| T-03-03-03 | Spoofing | og:url hardcoded domain | accept | Static SPA on Vercel; og:url is informational for social crawlers only. Domain will be confirmed in Phase 4. No auth or session state involved. |
</threat_model>

<verification>
```bash
# SEO meta present
grep "meta name=\"description\"" index.html
grep "meta name=\"viewport\"" index.html

# OG tags present
grep "og:title" index.html
grep "og:description" index.html
grep "og:image" index.html
grep "og:url" index.html

# Twitter Card present
grep "twitter:card" index.html

# Shimmer keyframe present
grep "keyframes shimmer" index.html

# Body untouched
grep "src/main.jsx" index.html
grep "id=\"root\"" index.html

# Title unchanged
grep "Cordy's Crafts · Hand-made with love" index.html
```
</verification>

<success_criteria>
- `grep "og:image" index.html` returns at least 2 matches (og:image and twitter:image)
- `grep "keyframes shimmer" index.html` returns 1 match
- `grep "meta name=\"description\"" index.html` returns 1 match
- `npm run dev` starts without errors
- Browser DevTools `<head>` shows all meta tags
- Skeleton animation plays when network is throttled (shimmer sweep visible on placeholder cards)
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-polish/03-03-SUMMARY.md`
</output>
