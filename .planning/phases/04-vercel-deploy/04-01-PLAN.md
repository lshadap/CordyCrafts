---
phase: 04-vercel-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DEPL-04

must_haves:
  truths:
    - "A developer who clones the repo can follow README instructions and run the site locally without asking Cordeelia for anything beyond the env var values"
    - "README documents all three env vars with their purpose and where to obtain values"
    - "README covers exactly: clone → npm install → .env.local → npm run dev"
  artifacts:
    - path: "README.md"
      provides: "Local setup documentation for future developers"
      contains: "## Local Setup"
    - path: "README.md"
      provides: "Env var reference table"
      contains: "VITE_SUPABASE_URL"
  key_links:
    - from: "README.md"
      to: ".env.example"
      via: "Instructs developer to copy .env.example to .env.local"
      pattern: "env.example"
    - from: "README.md"
      to: "npm run dev"
      via: "Local dev command listed in setup steps"
      pattern: "npm run dev"
---

<objective>
Write README.md with local setup instructions for future developers inheriting the project.

Purpose: Any developer who clones the repo can run the site locally by following the README — the only thing they need from Cordeelia is the 3 env var values (per D-05, D-06, D-07).
Output: README.md at repo root covering what the project is, prerequisites, local setup steps, env var reference, tech stack, project structure, and a one-line deployment note.
</objective>

<execution_context>
@/Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.planning/PROJECT.md
@/Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.planning/ROADMAP.md
@/Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/.planning/phases/04-vercel-deploy/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md</name>

  <read_first>
    - `.env.example` — source of truth for env var names and placeholder values
    - `vercel.json` — confirm SPA rewrite rule exists (mention in Deployment section)
    - `package.json` — confirm correct Node/npm versions and script names (`dev`, `build`)
  </read_first>

  <files>README.md</files>

  <action>
Create `README.md` at the repo root with exactly these sections in this order:

---

# Cordy's Crafts

Handmade crafts e-commerce site — browse paper crafts, clay, and candles, and book small-group workshops. Orders and bookings go straight to Cordeelia via WhatsApp; no payment gateway in v1.

## What It Is

React 18 + Vite 5 single-page app backed by Supabase. Customers browse the catalogue, add items to cart, and place orders via a pre-filled WhatsApp message. Built for the Indian market — all prices in ₹ (INR).

## Prerequisites

- Node.js 18 or higher
- npm (comes with Node.js)
- A code editor (VS Code recommended)

## Local Setup

1. Clone the repository:

   ```bash
   git clone https://github.com/YOUR_USERNAME/cordycrafts.git
   cd cordycrafts
   ```

2. Install dependencies:

   ```bash
   npm install
   ```

3. Copy the env var template:

   ```bash
   cp .env.example .env.local
   ```

4. Open `.env.local` and fill in the three values (get these from Cordeelia):

   ```
   VITE_SUPABASE_URL=https://xxxx.supabase.co
   VITE_SUPABASE_ANON_KEY=your-anon-key-here
   VITE_WHATSAPP_NUMBER=919876543210
   ```

5. Start the dev server:

   ```bash
   npm run dev
   ```

   The site runs at `http://localhost:5173`.

## Environment Variables

| Variable | Description | Where to find it |
|----------|-------------|-----------------|
| `VITE_SUPABASE_URL` | Supabase project URL | Supabase Dashboard → Settings → API → Project URL |
| `VITE_SUPABASE_ANON_KEY` | Supabase anon/public key | Supabase Dashboard → Settings → API → anon public key |
| `VITE_WHATSAPP_NUMBER` | WhatsApp number for orders/bookings (digits only, with country code, no `+`) | Ask Cordeelia |

All three values are required. The app throws a startup error listing any missing variable.

> **Note:** Use `.env.local` (not `.env`). Vite loads `.env.local` for local development and it is git-ignored by default.

## Tech Stack

| Layer | Technology |
|-------|-----------|
| UI framework | React 18.3 |
| Build tool | Vite 5 |
| Database | Supabase (PostgreSQL + RLS) |
| Hosting | Vercel |
| Styling | CSS custom properties (`colors_and_type.css`) + inline styles |

## Project Structure

```
src/
  primitives.jsx     # Reusable UI atoms — Icon, Logo, Button, CircleBadge
  hero.jsx           # Nav and Hero section
  products.jsx       # ProductGrid, ClassesGrid, and data hooks
  sections.jsx       # CartDrawer, BookingModal, About, Footer
  CartContext.jsx    # Cart state (useReducer + localStorage)
  App.jsx            # Root component
  main.jsx           # Entry point, env validation
  lib/
    supabase.js      # Supabase client (single instance)
    whatsapp.js      # wa.me URL builder
  hooks/
    useProducts.js   # Fetches products from Supabase
    useClasses.js    # Fetches classes from Supabase
    useBreakpoint.js # Responsive breakpoint hook
```

## Deployment

Deployed on Vercel at `https://cordyscrafts.vercel.app`. Auto-deploys from the `main` branch — push a commit to main and Vercel builds and deploys automatically.

---

IMPORTANT constraints:
- Use `.env.local` (not `.env`) throughout — that is the correct Vite convention
- Do NOT include Vercel deployment setup steps in the README (per D-07)
- Do NOT include Supabase project creation or migration steps (per D-06)
- The GitHub clone URL placeholder `YOUR_USERNAME/cordycrafts` is intentional — do not hardcode a specific username
  </action>

  <verify>
    <automated>grep -c "## Local Setup" /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/README.md && grep -c "VITE_SUPABASE_URL" /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/README.md && grep -c "VITE_WHATSAPP_NUMBER" /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/README.md && grep -c "npm run dev" /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/README.md && grep -c "npm install" /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/README.md && grep -c "env.example" /Users/lambertshadap/Personal/PersonalVentures/CordyCrafts/CordyCrafts/README.md</automated>
  </verify>

  <acceptance_criteria>
    - `README.md` exists at repo root
    - `grep "## Local Setup" README.md` returns a match
    - `grep "VITE_SUPABASE_URL" README.md` returns at least 2 matches (setup steps + env var table)
    - `grep "VITE_SUPABASE_ANON_KEY" README.md` returns at least 2 matches
    - `grep "VITE_WHATSAPP_NUMBER" README.md` returns at least 2 matches
    - `grep "npm install" README.md` returns a match
    - `grep "npm run dev" README.md` returns a match
    - `grep "\.env\.local" README.md` returns a match (Vite-correct filename, NOT `.env`)
    - `grep "\.env\.example" README.md` returns a match (cp command references source template)
    - `grep "## Deployment" README.md` returns a match
    - `grep "cordyscrafts.vercel.app" README.md` returns a match
    - File does NOT contain "vercel login", "vercel --prod", or "npx vercel" (no deploy steps per D-07)
    - File does NOT contain "supabase migration" or "npx supabase" (no migration steps per D-06)
  </acceptance_criteria>

  <done>README.md exists at repo root, all acceptance criteria pass via grep, file is committed to git.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → developer | README content is untrusted documentation read by humans, not executed code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | README.md | accept | README contains no secrets — only env var names (not values) and public URLs. `.env.local` is git-ignored; README explicitly instructs using `.env.local` not `.env` to prevent accidental commit of secrets. |
| T-04-02 | Tampering | README.md | accept | README is documentation only; no executable code. Malicious edits would require repo write access (controlled via GitHub permissions). Low-value attack vector. |
</threat_model>

<verification>
After task completion:

1. `README.md` exists: `ls README.md` returns the file
2. All required sections present: grep checks for `## Local Setup`, `## Environment Variables`, `## Tech Stack`, `## Project Structure`, `## Deployment`
3. All env var names present: grep for `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_WHATSAPP_NUMBER`
4. No forbidden content: grep confirms no `vercel login`, `npx vercel`, `supabase migration` strings
5. Uses `.env.local` (not `.env`): grep for `\.env\.local`
</verification>

<success_criteria>
README.md at repo root, containing all 7 sections, all 3 env var names, local dev steps (clone → npm install → .env.local → npm run dev), and no Vercel deploy steps or Supabase migration steps.
</success_criteria>

<output>
After completion, create `.planning/phases/04-vercel-deploy/04-01-SUMMARY.md`
</output>
